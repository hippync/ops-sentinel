# Sandbox app
DB_URL=jdbc:postgresql://localhost:5432/orders
DB_USER=orders
DB_PASSWORD=changeme
CHAOS_ENABLED=false
CHAOS_ADMIN_API_KEY=

# Agents
ANTHROPIC_API_KEY=
AWS_REGION=us-east-1
AWS_PROFILE=ops-sentinel-sandbox

# Guardrails — enforced outside the LLM's control (risk rows 3 and 4)
WORKER_MAX_ITERATIONS=5
MAX_DESIRED_COUNT=4
