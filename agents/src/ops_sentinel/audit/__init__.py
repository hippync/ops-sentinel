"""Structured decision log.

One AuditRecord per run: the alarm, the triage classification, every Worker
iteration, the full Validator verdict with per-rule outcomes, the approval record,
and the execution result.

A v1 deliverable shown in the demo, not a debugging aid.
"""
